import { type NextPage } from "next";

const Search: NextPage = () => {
  return (
    <main className="bg-base-100">
      <section className="container mx-auto flex min-h-screen flex-col">
        search something
      </section>
    </main>
  );
};

export default Search;
